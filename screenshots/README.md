# Screenshots & demo media

Media used by the main [README](../README.md), captured against a small two-role JWT
demo API.

| File | Shows |
|------|-------|
| `rolebreaker-demo.gif` | Full walkthrough (2× speed): login as both roles → Auto sweep → matrix → findings → HMAC crack |
| `01-overview.png` | The RoleBreaker tab, clean starting state |
| `02-results.png` | Populated: personas, discovered identities, and the access matrix (broken endpoints amber on the `user` column, the properly-enforced `delete` green/403) |

> The raw recording (`demo-raw.mov`) and the full-quality `rolebreaker-demo.mp4`
> are git-ignored — only the optimized GIF and PNGs are committed.
